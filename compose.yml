version: '3.8'

services:
  clashdrive:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: clashdrive
    restart: unless-stopped
    ports:
      - "5173:5173"
    command: ["bun", "x", "vite", "preview", "--host", "0.0.0.0", "--port", "5173", "--allowed-hosts", "all"]
